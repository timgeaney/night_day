
  <div class="item">
      <%= link_to (image_tag event.photo.url(:medium)), event_path(event) %>
      <!-- <p><%= event.description %></p>
      <p>Posted by <%= event.user.full_name %></p>
      <p>Date: <%= event.date %></p>
      <p>Time: <%= event.time.to_s %></p> -->
  </div>
